# Environment variables for the client
# For local development, the Vite dev server proxies /api requests to localhost:5000
# For production (Vercel), leave this empty to use relative paths

VITE_API_URL=
